# Use special escape sequences to avoid problems with special characters:
#   %?0% - expands to ^ (Useful when "evaluate" flag defined)
#   %?1% - expands to " (Should always be used instead)
#   !?2! - expands to ! (Should always be used in case of "setlocal ENABLEDELAYEDEXPANSION" before variables loading) 
"CYGWIN_PATH=c:\Cygwin\1.5.x"
"CYGWIN_REGKEY_PATH=HKEY_LOCAL_MACHINE\SOFTWARE\Cygnus Solutions\Cygwin\mounts v2"
# In case if the Mingw binaries used under the Cygwin platform
#"MINGW_PATH=c:\mingw"
